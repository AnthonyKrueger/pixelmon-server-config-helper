import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
          main: '#e8631b',
        },
        secondary: {
          main: '#03a9f4',
        },
     }        
});

export default theme;